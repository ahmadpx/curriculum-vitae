import React from 'react';
import { storiesOf } from '@storybook/react';
import Experience from './Experience';

storiesOf('components/Experience', module).add('default', () => (
  <Experience />
));
