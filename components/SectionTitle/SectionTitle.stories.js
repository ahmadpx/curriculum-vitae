import React from 'react';
import { storiesOf } from '@storybook/react';
import SectionTitle from './SectionTitle';

storiesOf('components/SectionTitle', module).add('default', () => (
  <SectionTitle />
));
